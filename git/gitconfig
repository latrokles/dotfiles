# This is Git's per-user configuration file.
[core]
# Please adapt and uncomment the following lines:
    user = latrokles
    email = latrokles@gmail.com
[user]
	name = latrokles
	email = latrokles@gmail.com
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
