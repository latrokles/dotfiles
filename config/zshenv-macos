export DEVENV=${HOME}/dev/src/latrokles
export DOTFILES=${DEVENV}/dotfiles

source ${DOTFILES}/config/aliases-common
source ${DOTFILES}/config/aliases-macos

export PATH=${HOME}/.local/bin:${PATH}
. "$HOME/.cargo/env"
